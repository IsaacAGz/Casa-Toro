---
// src/components/LangToggle.astro
const { url } = Astro;
const { pathname } = url;

// Determine current language based on the URL pathway prefix
const isSpanish = pathname.startsWith('/es');

// Swap the URL prefix dynamically while preserving the rest of the page route path
const targetPath = isSpanish 
  ? pathname.replace('/es', '/en') 
  : pathname.startsWith('/en') 
    ? pathname.replace('/en', '/es')
    : `/es${pathname}`; // Fallback if root path
---

<a 
  href={targetPath}
  class="font-sans text-[11px] tracking-widest uppercase text-[var(--color-text-main)] font-semibold border border-[var(--color-border-main)] px-2.5 py-1 transition-all hover:bg-[var(--color-accent-hover)] hover:text-[var(--color-text-light)]"
>
  {isSpanish ? 'EN' : 'ES'}
</a>